import 'package:kas_app/app/models/register.dart';

class RegisterCrew {
  final DateTime date;
  final List<Register> registers;
  RegisterCrew({
    required this.date,
    required this.registers,
  });
}
